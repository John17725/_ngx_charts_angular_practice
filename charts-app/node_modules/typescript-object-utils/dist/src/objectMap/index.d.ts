export declare function objectMap<A, B, T extends {
    [id: string]: A;
}>(obj: T, cb: Processor<A, B, T>): {
    [P in keyof T]: B;
};
export declare type Processor<A, B, T extends {
    [id: string]: A;
}> = (v: A, key: keyof T, obj: T) => B;
